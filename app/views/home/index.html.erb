<h2>新添加云应用</h2>
<div class="apps">
  <ul class="media-grid">
  <% @apps.each do |app| %>
    <li>
      <%= link_to(image_tag(app.snapshot_url(:large), :class => "thumbnail"), app)%>
    </li>
  <% end %>
  </ul>
<%= paginate @apps %>
</div>
<% content_for :sidebar do%>
  <%= render :partial => "common/add_node_link" %>
<% end %>

