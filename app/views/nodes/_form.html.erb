<%= simple_form_for(@node, :html => { :class => "form-horizontal" }) do |f| %>
  <%= f.input :name %>
  <%= f.input :summary %>
  <%= f.input :sort %>
  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
